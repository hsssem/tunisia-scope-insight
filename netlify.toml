[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  DEPLOY_TARGET = "netlify"
  NODE_VERSION = "22"
  NPM_FLAGS = "--include=dev"

[dev]
  command = "npm run dev"
  port = 8888
  targetPort = 8080
